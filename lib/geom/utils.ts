import { Vec3 } from "./geomTypes";

export function determineStartingVec3(
  verticesA: Vec3[],
  verticesB: Vec3[]
): Vec3 {
  // loop through the vertices and find the ones in the separate arrays
}
